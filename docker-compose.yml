name: readarc
services:
  relay:
    build:
      context: ./server/rendezvous_relay
    image: readarc-relay:dev
    ports:
      - "127.0.0.1:8787:8787"
    environment:
      PORT: "8787"
    restart: unless-stopped
