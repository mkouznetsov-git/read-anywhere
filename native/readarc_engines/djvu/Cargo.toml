[package]
name = "readarc_djvu_engine"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
# MIT licensed pure-Rust decoder. This is the selected production path instead
# of GPL DjVuLibre command-line tools.
djvu-rs = "0.10"
